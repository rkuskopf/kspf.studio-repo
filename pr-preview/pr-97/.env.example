# Existing static Storyblok setup, sync, and preview tooling.
# Next.js uses the least-privilege example in next-app/.env.example instead.
STORYBLOK_SPACE_ID=
STORYBLOK_REGION=eu
STORYBLOK_PUBLIC_TOKEN=
STORYBLOK_PREVIEW_TOKEN=
STORYBLOK_MANAGEMENT_TOKEN=
